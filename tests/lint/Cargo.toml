[package]
name = "lint"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "lib.rs"

[dependencies]
pin-project = { path = "../.." }

[lints]
# Set at the crate root.
# workspace = true
