[package]
name = "expandtest"
version = "0.1.0"
edition = "2018"
publish = false

[dependencies]

[dev-dependencies]
pin-project = { path = "../.." }
macrotest = "1.0.9"
prettyplease = "=0.1.15"

# For test on MSRV.
diff = "=0.1.12"
