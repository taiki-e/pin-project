[package]
name = "no-core"
version = "0.0.0"
edition = "2021"
rust-version = "1.71" # Prevent clippy from suggesting a code that requires a new version.
publish = false

[lib]
path = "lib.rs"

[dependencies]
pin-project = { path = "../.." }

[lints]
workspace = true
